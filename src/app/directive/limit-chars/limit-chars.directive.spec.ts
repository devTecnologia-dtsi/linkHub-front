import { LimitCharsDirective } from './limit-chars.directive';

describe('LimitCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitCharsDirective();
    expect(directive).toBeTruthy();
  });
});
